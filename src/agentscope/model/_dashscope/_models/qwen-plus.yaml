name: qwen-plus
label: Qwen Plus
status: active

input_types:
  - text/plain

output_types:
  - text/plain
  - application/x-thinking

context_size: 1000000
output_size: 16384

parameter_overrides:
  max_tokens: {"maximum": 16384}
  # Voice only applies to omni-style models; hide the field for this
  # non-omni model so the frontend popover doesn't render it.
  voice:
    hidden: true
