name: qwen3.6-max-preview
label: Qwen3.6-Max-Preview
status: active

input_types:
  - text/plain

output_types:
  - text/plain
  - application/x-thinking

context_size: 240000
output_size: 64000

parameter_overrides:
  max_tokens: {"maximum": 64000}
  # Voice only applies to omni-style models; hide the field for this
  # non-omni model so the frontend popover doesn't render it.
  voice:
    hidden: true
